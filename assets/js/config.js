// API конфигурация
const API_CONFIG = {
  YANDEX_MAPS_V2_KEY: 'YANDEX_API_KEY',
  YANDEX_MAPS_V3_KEY: 'c0e28937-4e16-4d15-9b99-09a30cb8fc13'
};

const ROUTE_DATA = {
  Gold: {
    hex: '#FFD700', label: 'Маневры на перекрестке',
    commands: ["На перекрестке повернем налево", "На перекрестке едем прямо", "На перекрестке повернем направо", "На перекрестке выполним разворот", "На круговом движении первый съезд", "На круговом движении второй съезд", "На круговом движении третий съезд", "На круговом движении четвертый съезд", "На круговом движении выполним разворот", "На регулируемом перекрестке повернем налево", "На регулируемом перекрестке едем прямо", "На регулируемом перекрестке повернем направо", "На регулируемом перекрестке выполним разворот", "На нерегулируемом перекрестке повернем налево", "На нерегулируемом перекрестке едем прямо", "На нерегулируемом перекрестке повернем направо", "На нерегулируемом перекрестке выполним разворот", "Выполним разворот в ближайшем разрешенном месте", "На перекрестке повернем налево, а на следующем направо", "На перекрестке повернем направо, а на следующем налево"]
  },
  Blue: {
    hex: '#007AFF', label: 'Разворот вне перекрестка',
    commands: ["Выполним разворот вне перекрестка", "Выполним разворот в ближайшем разрешенном месте", "Найдите место для разворота и развернитесь"]
  },
  Red: {
    hex: '#FF3B30', label: 'Разгон до максимальной скорости',
    commands: ["Выполняем разгон до максимальной скорости", "Набираем максимальную скорость на данном участке дороги", "Разгоняемся до максимальной разрешенной скорости"]
  },
  Fuchsia: {
    hex: '#AF52DE', label: 'Остановка и начало движения на подъем',
    commands: ["По моей команде выполним остановку и начало движения на подъеме", "Выполняем остановку и начало движения на подъеме"]
  },
  Orange: {
    hex: '#FF9500', label: 'Левые и правые повороты',
    commands: ["Поворачиваем направо", "Поворачиваем налево", "Далее повернем направо", "Далее повернем налево", "На светофоре повернем направо", "На светофоре повернем налево", "Поворачиваем направо к ленте", "Поворачиваем направо на заправку", "Едем в прямом направлении"]
  },
  Purple: {
    hex: '#5856D6', label: 'Параллельная парковка и гараж',
    commands: ["Выполняем параллельную парковку и гараж"]
  },
  Cyan: {
    hex: '#5AC8FA', label: 'Разворот в ограниченном пространстве',
    commands: ["Выполняем разворот в ограниченном пространстве", "Выполним разворот в ограниченном пространстве с использованием передачи заднего хода"]
  },
  Brown: {
    hex: '#A2845E', label: 'Остановка',
    commands: ["Выполняем остановку параллельно краю проезжей части", "Выполняем остановку в ближайшем разрешенном месте"]
  },
  Lime: {
    hex: '#34C759', label: 'Начало движения',
    commands: ["Начинаем движение", "Как будете готовы начинаем движение"]
  }
};

const COLORS = Object.fromEntries(
  Object.entries(ROUTE_DATA).map(([k, v]) => [k, { hex: v.hex, label: v.label }])
);

const COMMAND_SETS = Object.fromEntries(
  Object.entries(ROUTE_DATA).map(([k, v]) => [k, [...v.commands]])
);

const ALL_AUDIO_COMMANDS = [...new Set(Object.values(ROUTE_DATA).flatMap(v => v.commands))];